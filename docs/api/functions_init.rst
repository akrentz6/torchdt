Parameter initialization
========================

Initialization mutates the supplied custom tensor. Random initializers generate floating-point values and encode them; constant initializers fill encoded values directly.

.. torchdt-functions:: init_funcs
