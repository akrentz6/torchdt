Loss functions
==============

Call backward on the encoded loss. Class-index targets remain ordinary integer tensors; regression targets use the same custom format as predictions. Reduction options, where present, are ``none``, ``mean``, and ``sum``.

.. torchdt-functions:: loss_funcs
